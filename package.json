{
  "name": "vaya",
  "version": "0.1.0",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.3",
    "typescript": "^5.7.0",
    "vite": "^8.1.0"
  },
  "description": "Game video auto-editing desktop app",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build:frontend": "vite build",
    "typecheck": "tsc --noEmit",
    "tauri": "tauri",
    "start": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
